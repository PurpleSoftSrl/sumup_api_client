// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'financialpayoutstatus.dart';
import 'financialpayouttype.dart';

/// Object model for `FinancialPayout` from the SumUp API spec.
class FinancialPayout {
  const FinancialPayout({
    required this.id,
    required this.type_,
    required this.amount,
    required this.date,
    required this.currency,
    required this.fee,
    required this.status,
    required this.reference,
    required this.transactionCode,
  });

  final int id;
  final FinancialPayoutType type_;
  final double amount;
  final DateTime date;
  final String currency;
  final double fee;
  final FinancialPayoutStatus status;
  final String reference;
  final String transactionCode;

  factory FinancialPayout.fromJson(Map<String, dynamic> json) {
    return FinancialPayout(
      id: (json['id'] as num).toInt(),
      type_: FinancialPayoutType.fromJson(json['type'] as String),
      amount: (json['amount'] as num).toDouble(),
      date: DateTime.parse(json['date'] as String),
      currency: json['currency'] as String,
      fee: (json['fee'] as num).toDouble(),
      status: FinancialPayoutStatus.fromJson(json['status'] as String),
      reference: json['reference'] as String,
      transactionCode: json['transaction_code'] as String,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'type': type_.toJson(),
      'amount': amount,
      'date': date.toIso8601String(),
      'currency': currency,
      'fee': fee,
      'status': status.toJson(),
      'reference': reference,
      'transaction_code': transactionCode,
    };
  }

  FinancialPayout copyWith({
    int? id,
    FinancialPayoutType? type_,
    double? amount,
    DateTime? date,
    String? currency,
    double? fee,
    FinancialPayoutStatus? status,
    String? reference,
    String? transactionCode,
  }) {
    if (id == null && type_ == null && amount == null && date == null && currency == null && fee == null && status == null && reference == null && transactionCode == null) return this;

    return FinancialPayout(
      id: id ?? this.id,
      type_: type_ ?? this.type_,
      amount: amount ?? this.amount,
      date: date ?? this.date,
      currency: currency ?? this.currency,
      fee: fee ?? this.fee,
      status: status ?? this.status,
      reference: reference ?? this.reference,
      transactionCode: transactionCode ?? this.transactionCode,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    if (other is! FinancialPayout) return false;
    return id == other.id
        && type_ == other.type_
        && amount == other.amount
        && date == other.date
        && currency == other.currency
        && fee == other.fee
        && status == other.status
        && reference == other.reference
        && transactionCode == other.transactionCode
;
  }

  @override
  int get hashCode => Object.hash(id, type_, amount, date, currency, fee, status, reference, transactionCode);

  @override
  String toString() => 'FinancialPayout(id=$id, type_=$type_, amount=$amount, date=$date, currency=$currency, fee=$fee, status=$status, reference=$reference, transactionCode=$transactionCode)';
}
