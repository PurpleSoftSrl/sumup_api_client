// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//

/// Object model for `ReceiptTransactionProductsItem` from the SumUp API spec.
class ReceiptTransactionProductsItem {
  const ReceiptTransactionProductsItem({
    this.name,
    this.description,
    this.price,
    this.vatRate,
    this.singleVatAmount,
    this.priceWithVat,
    this.vatAmount,
    this.quantity,
    this.totalPrice,
    this.totalWithVat,
  });

  final String? name;
  final String? description;
  final String? price;
  final String? vatRate;
  final String? singleVatAmount;
  final String? priceWithVat;
  final String? vatAmount;
  final int? quantity;
  final String? totalPrice;
  final String? totalWithVat;

  /// Creates an instance from a JSON map.
  factory ReceiptTransactionProductsItem.fromJson(Map<String, dynamic> json) {
    return ReceiptTransactionProductsItem(
      name: json['name'] != null ? json['name'] as String : null,
      description:
          json['description'] != null ? json['description'] as String : null,
      price: json['price'] != null ? json['price'] as String : null,
      vatRate: json['vat_rate'] != null ? json['vat_rate'] as String : null,
      singleVatAmount: json['single_vat_amount'] != null
          ? json['single_vat_amount'] as String
          : null,
      priceWithVat: json['price_with_vat'] != null
          ? json['price_with_vat'] as String
          : null,
      vatAmount:
          json['vat_amount'] != null ? json['vat_amount'] as String : null,
      quantity:
          json['quantity'] != null ? (json['quantity'] as num).toInt() : null,
      totalPrice:
          json['total_price'] != null ? json['total_price'] as String : null,
      totalWithVat: json['total_with_vat'] != null
          ? json['total_with_vat'] as String
          : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      if (name != null) 'name': name!,
      if (description != null) 'description': description!,
      if (price != null) 'price': price!,
      if (vatRate != null) 'vat_rate': vatRate!,
      if (singleVatAmount != null) 'single_vat_amount': singleVatAmount!,
      if (priceWithVat != null) 'price_with_vat': priceWithVat!,
      if (vatAmount != null) 'vat_amount': vatAmount!,
      if (quantity != null) 'quantity': quantity!,
      if (totalPrice != null) 'total_price': totalPrice!,
      if (totalWithVat != null) 'total_with_vat': totalWithVat!,
    };
  }

  ReceiptTransactionProductsItem copyWith({
    String? name,
    String? description,
    String? price,
    String? vatRate,
    String? singleVatAmount,
    String? priceWithVat,
    String? vatAmount,
    int? quantity,
    String? totalPrice,
    String? totalWithVat,
  }) {
    if (name == null &&
        description == null &&
        price == null &&
        vatRate == null &&
        singleVatAmount == null &&
        priceWithVat == null &&
        vatAmount == null &&
        quantity == null &&
        totalPrice == null &&
        totalWithVat == null) {
      return this;
    }

    return ReceiptTransactionProductsItem(
      name: name ?? this.name,
      description: description ?? this.description,
      price: price ?? this.price,
      vatRate: vatRate ?? this.vatRate,
      singleVatAmount: singleVatAmount ?? this.singleVatAmount,
      priceWithVat: priceWithVat ?? this.priceWithVat,
      vatAmount: vatAmount ?? this.vatAmount,
      quantity: quantity ?? this.quantity,
      totalPrice: totalPrice ?? this.totalPrice,
      totalWithVat: totalWithVat ?? this.totalWithVat,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! ReceiptTransactionProductsItem) {
      return false;
    }
    return name == other.name &&
        description == other.description &&
        price == other.price &&
        vatRate == other.vatRate &&
        singleVatAmount == other.singleVatAmount &&
        priceWithVat == other.priceWithVat &&
        vatAmount == other.vatAmount &&
        quantity == other.quantity &&
        totalPrice == other.totalPrice &&
        totalWithVat == other.totalWithVat;
  }

  @override
  int get hashCode => Object.hash(
      name,
      description,
      price,
      vatRate,
      singleVatAmount,
      priceWithVat,
      vatAmount,
      quantity,
      totalPrice,
      totalWithVat);

  @override
  String toString() =>
      'ReceiptTransactionProductsItem(name=$name, description=$description, price=$price, vatRate=$vatRate, singleVatAmount=$singleVatAmount, priceWithVat=$priceWithVat, vatAmount=$vatAmount, quantity=$quantity, totalPrice=$totalPrice, totalWithVat=$totalWithVat)';
}
