// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'card.dart';
import 'mandatepayload.dart';
import 'personaldetails.dart';
import 'processcheckoutapplepay.dart';
import 'processcheckoutgooglepay.dart';
import 'processcheckoutpaymenttype.dart';

/// Object model for `ProcessCheckout` from the SumUp API spec.
class ProcessCheckout {
  const ProcessCheckout({
    required this.paymentType,
    this.installments,
    this.mandate,
    this.card,
    this.googlePay,
    this.applePay,
    this.token,
    this.customerId,
    this.personalDetails,
  });

  final ProcessCheckoutPaymentType paymentType;
  final int? installments;
  final MandatePayload? mandate;
  final Card? card;
  final ProcessCheckoutGooglePay? googlePay;
  final ProcessCheckoutApplePay? applePay;
  final String? token;
  final String? customerId;
  final PersonalDetails? personalDetails;

  factory ProcessCheckout.fromJson(Map<String, dynamic> json) {
    return ProcessCheckout(
      paymentType:
          ProcessCheckoutPaymentType.fromJson(json['payment_type'] as String),
      installments: json['installments'] != null
          ? (json['installments'] as num).toInt()
          : null,
      mandate: json['mandate'] != null
          ? MandatePayload.fromJson(json['mandate'] as Map<String, dynamic>)
          : null,
      card: json['card'] != null
          ? Card.fromJson(json['card'] as Map<String, dynamic>)
          : null,
      googlePay: json['google_pay'] != null
          ? ProcessCheckoutGooglePay.fromJson(
              json['google_pay'] as Map<String, dynamic>)
          : null,
      applePay: json['apple_pay'] != null
          ? ProcessCheckoutApplePay.fromJson(
              json['apple_pay'] as Map<String, dynamic>)
          : null,
      token: json['token'] != null ? json['token'] as String : null,
      customerId:
          json['customer_id'] != null ? json['customer_id'] as String : null,
      personalDetails: json['personal_details'] != null
          ? PersonalDetails.fromJson(
              json['personal_details'] as Map<String, dynamic>)
          : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      'payment_type': paymentType.toJson(),
      if (installments != null) 'installments': installments!,
      if (mandate != null) 'mandate': mandate!.toJson(),
      if (card != null) 'card': card!.toJson(),
      if (googlePay != null) 'google_pay': googlePay!.toJson(),
      if (applePay != null) 'apple_pay': applePay!.toJson(),
      if (token != null) 'token': token!,
      if (customerId != null) 'customer_id': customerId!,
      if (personalDetails != null)
        'personal_details': personalDetails!.toJson(),
    };
  }

  ProcessCheckout copyWith({
    ProcessCheckoutPaymentType? paymentType,
    int? installments,
    MandatePayload? mandate,
    Card? card,
    ProcessCheckoutGooglePay? googlePay,
    ProcessCheckoutApplePay? applePay,
    String? token,
    String? customerId,
    PersonalDetails? personalDetails,
  }) {
    if (paymentType == null &&
        installments == null &&
        mandate == null &&
        card == null &&
        googlePay == null &&
        applePay == null &&
        token == null &&
        customerId == null &&
        personalDetails == null) return this;

    return ProcessCheckout(
      paymentType: paymentType ?? this.paymentType,
      installments: installments ?? this.installments,
      mandate: mandate ?? this.mandate,
      card: card ?? this.card,
      googlePay: googlePay ?? this.googlePay,
      applePay: applePay ?? this.applePay,
      token: token ?? this.token,
      customerId: customerId ?? this.customerId,
      personalDetails: personalDetails ?? this.personalDetails,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    if (other is! ProcessCheckout) return false;
    return paymentType == other.paymentType &&
        installments == other.installments &&
        mandate == other.mandate &&
        card == other.card &&
        googlePay == other.googlePay &&
        applePay == other.applePay &&
        token == other.token &&
        customerId == other.customerId &&
        personalDetails == other.personalDetails;
  }

  @override
  int get hashCode => Object.hash(paymentType, installments, mandate, card,
      googlePay, applePay, token, customerId, personalDetails);

  @override
  String toString() =>
      'ProcessCheckout(paymentType=$paymentType, installments=$installments, mandate=$mandate, card=$card, googlePay=$googlePay, applePay=$applePay, token=$token, customerId=$customerId, personalDetails=$personalDetails)';
}
