// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by openapi_flutter_gen
//
import 'receiptcard.dart';
import 'receiptevent.dart';
import 'receiptreader.dart';
import 'receipttransactionprocessas.dart';
import 'receipttransactionproductsitem.dart';
import 'receipttransactionvatratesitem.dart';

/// Transaction information.
class ReceiptTransaction {
  const ReceiptTransaction({
    this.transactionCode,
    this.transactionId,
    this.merchantCode,
    this.amount,
    this.vatAmount,
    this.tipAmount,
    this.currency,
    this.timestamp,
    this.status,
    this.paymentType,
    this.entryMode,
    this.verificationMethod,
    this.cardReader,
    this.card,
    this.installmentsCount,
    this.processAs,
    this.products,
    this.vatRates,
    this.events,
    this.receiptNo,
  });

  final String? transactionCode;
  final String? transactionId;
  final String? merchantCode;
  final String? amount;
  final String? vatAmount;
  final String? tipAmount;
  final String? currency;
  final DateTime? timestamp;
  final String? status;
  final String? paymentType;
  final String? entryMode;
  final String? verificationMethod;
  final ReceiptReader? cardReader;
  final ReceiptCard? card;
  final int? installmentsCount;
  final ReceiptTransactionProcessAs? processAs;
  final List<ReceiptTransactionProductsItem>? products;
  final List<ReceiptTransactionVatRatesItem>? vatRates;
  final List<ReceiptEvent>? events;
  final String? receiptNo;

  factory ReceiptTransaction.fromJson(Map<String, dynamic> json) {
    return ReceiptTransaction(
      transactionCode: json['transaction_code'] != null
          ? json['transaction_code'] as String
          : null,
      transactionId: json['transaction_id'] != null
          ? json['transaction_id'] as String
          : null,
      merchantCode: json['merchant_code'] != null
          ? json['merchant_code'] as String
          : null,
      amount: json['amount'] != null ? json['amount'] as String : null,
      vatAmount:
          json['vat_amount'] != null ? json['vat_amount'] as String : null,
      tipAmount:
          json['tip_amount'] != null ? json['tip_amount'] as String : null,
      currency: json['currency'] != null ? json['currency'] as String : null,
      timestamp: json['timestamp'] != null
          ? DateTime.parse(json['timestamp'] as String)
          : null,
      status: json['status'] != null ? json['status'] as String : null,
      paymentType:
          json['payment_type'] != null ? json['payment_type'] as String : null,
      entryMode:
          json['entry_mode'] != null ? json['entry_mode'] as String : null,
      verificationMethod: json['verification_method'] != null
          ? json['verification_method'] as String
          : null,
      cardReader: json['card_reader'] != null
          ? ReceiptReader.fromJson(json['card_reader'] as Map<String, dynamic>)
          : null,
      card: json['card'] != null
          ? ReceiptCard.fromJson(json['card'] as Map<String, dynamic>)
          : null,
      installmentsCount: json['installments_count'] != null
          ? (json['installments_count'] as num).toInt()
          : null,
      processAs: json['process_as'] != null
          ? ReceiptTransactionProcessAs.fromJson(json['process_as'] as String)
          : null,
      products: json['products'] != null
          ? List<ReceiptTransactionProductsItem>.generate(
              (json['products'] as List).length,
              (i) => ReceiptTransactionProductsItem.fromJson(
                  (json['products'] as List)[i] as Map<String, dynamic>),
              growable: false)
          : null,
      vatRates: json['vat_rates'] != null
          ? List<ReceiptTransactionVatRatesItem>.generate(
              (json['vat_rates'] as List).length,
              (i) => ReceiptTransactionVatRatesItem.fromJson(
                  (json['vat_rates'] as List)[i] as Map<String, dynamic>),
              growable: false)
          : null,
      events: json['events'] != null
          ? List<ReceiptEvent>.generate(
              (json['events'] as List).length,
              (i) => ReceiptEvent.fromJson(
                  (json['events'] as List)[i] as Map<String, dynamic>),
              growable: false)
          : null,
      receiptNo:
          json['receipt_no'] != null ? json['receipt_no'] as String : null,
    );
  }

  Map<String, dynamic> toJson() {
    return {
      if (transactionCode != null) 'transaction_code': transactionCode!,
      if (transactionId != null) 'transaction_id': transactionId!,
      if (merchantCode != null) 'merchant_code': merchantCode!,
      if (amount != null) 'amount': amount!,
      if (vatAmount != null) 'vat_amount': vatAmount!,
      if (tipAmount != null) 'tip_amount': tipAmount!,
      if (currency != null) 'currency': currency!,
      if (timestamp != null) 'timestamp': timestamp!.toIso8601String(),
      if (status != null) 'status': status!,
      if (paymentType != null) 'payment_type': paymentType!,
      if (entryMode != null) 'entry_mode': entryMode!,
      if (verificationMethod != null)
        'verification_method': verificationMethod!,
      if (cardReader != null) 'card_reader': cardReader!.toJson(),
      if (card != null) 'card': card!.toJson(),
      if (installmentsCount != null) 'installments_count': installmentsCount!,
      if (processAs != null) 'process_as': processAs!.toJson(),
      if (products != null)
        'products': products!.map((e) => e.toJson()).toList(),
      if (vatRates != null)
        'vat_rates': vatRates!.map((e) => e.toJson()).toList(),
      if (events != null) 'events': events!.map((e) => e.toJson()).toList(),
      if (receiptNo != null) 'receipt_no': receiptNo!,
    };
  }

  ReceiptTransaction copyWith({
    String? transactionCode,
    String? transactionId,
    String? merchantCode,
    String? amount,
    String? vatAmount,
    String? tipAmount,
    String? currency,
    DateTime? timestamp,
    String? status,
    String? paymentType,
    String? entryMode,
    String? verificationMethod,
    ReceiptReader? cardReader,
    ReceiptCard? card,
    int? installmentsCount,
    ReceiptTransactionProcessAs? processAs,
    List<ReceiptTransactionProductsItem>? products,
    List<ReceiptTransactionVatRatesItem>? vatRates,
    List<ReceiptEvent>? events,
    String? receiptNo,
  }) {
    if (transactionCode == null &&
        transactionId == null &&
        merchantCode == null &&
        amount == null &&
        vatAmount == null &&
        tipAmount == null &&
        currency == null &&
        timestamp == null &&
        status == null &&
        paymentType == null &&
        entryMode == null &&
        verificationMethod == null &&
        cardReader == null &&
        card == null &&
        installmentsCount == null &&
        processAs == null &&
        products == null &&
        vatRates == null &&
        events == null &&
        receiptNo == null) {
      return this;
    }

    return ReceiptTransaction(
      transactionCode: transactionCode ?? this.transactionCode,
      transactionId: transactionId ?? this.transactionId,
      merchantCode: merchantCode ?? this.merchantCode,
      amount: amount ?? this.amount,
      vatAmount: vatAmount ?? this.vatAmount,
      tipAmount: tipAmount ?? this.tipAmount,
      currency: currency ?? this.currency,
      timestamp: timestamp ?? this.timestamp,
      status: status ?? this.status,
      paymentType: paymentType ?? this.paymentType,
      entryMode: entryMode ?? this.entryMode,
      verificationMethod: verificationMethod ?? this.verificationMethod,
      cardReader: cardReader ?? this.cardReader,
      card: card ?? this.card,
      installmentsCount: installmentsCount ?? this.installmentsCount,
      processAs: processAs ?? this.processAs,
      products: products ?? this.products,
      vatRates: vatRates ?? this.vatRates,
      events: events ?? this.events,
      receiptNo: receiptNo ?? this.receiptNo,
    );
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) {
      return true;
    }
    if (other is! ReceiptTransaction) {
      return false;
    }
    if (hashCode != other.hashCode) {
      return false;
    }
    return transactionCode == other.transactionCode &&
        transactionId == other.transactionId &&
        merchantCode == other.merchantCode &&
        amount == other.amount &&
        vatAmount == other.vatAmount &&
        tipAmount == other.tipAmount &&
        currency == other.currency &&
        timestamp == other.timestamp &&
        status == other.status &&
        paymentType == other.paymentType &&
        entryMode == other.entryMode &&
        verificationMethod == other.verificationMethod &&
        cardReader == other.cardReader &&
        card == other.card &&
        installmentsCount == other.installmentsCount &&
        processAs == other.processAs &&
        products == other.products &&
        vatRates == other.vatRates &&
        events == other.events &&
        receiptNo == other.receiptNo;
  }

  @override
  int get hashCode => Object.hash(
      transactionCode,
      transactionId,
      merchantCode,
      amount,
      vatAmount,
      tipAmount,
      currency,
      timestamp,
      status,
      paymentType,
      entryMode,
      verificationMethod,
      cardReader,
      card,
      installmentsCount,
      processAs,
      products,
      vatRates,
      events,
      receiptNo);

  @override
  String toString() =>
      'ReceiptTransaction(transactionCode=$transactionCode, transactionId=$transactionId, merchantCode=$merchantCode, amount=$amount, vatAmount=$vatAmount, tipAmount=$tipAmount, currency=$currency, timestamp=$timestamp, status=$status, paymentType=$paymentType, entryMode=$entryMode, verificationMethod=$verificationMethod, cardReader=$cardReader, card=$card, installmentsCount=$installmentsCount, processAs=$processAs, products=$products, vatRates=$vatRates, events=$events, receiptNo=$receiptNo)';
}
